version: "3.9"
services:
  postgres:
    container_name: learn-language-postgres
    networks:
      - proxynet
    image: postgres:14
    restart: always
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "secret"
      POSTGRES_DB: "learn-language"
    ports:
      - "5432:5432"
    expose:
      - "5432"

networks:
  proxynet:
    name: learn-language-network
